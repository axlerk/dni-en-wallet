# Identificadores de Apple Developer
PASS_TYPE_ID=pass.studio.onegoodman.dni
TEAM_ID=ABCDE12345
ORG_NAME=OneGoodMan Studio

# Carpeta con wwdr.pem, signerCert.pem y signerKey.pem (relativa a la raíz del proyecto)
CERT_DIR=certs
# Contraseña de la clave privada, si se puso al exportar el .p12
SIGNER_KEY_PASSPHRASE=

# Alternativa a la carpeta certs para hosting sin disco: el PEM en base64 (base64 -i certs/wwdr.pem | tr -d '\n')
# WWDR_PEM_B64=
# SIGNER_CERT_PEM_B64=
# SIGNER_KEY_PEM_B64=

PORT=8787
